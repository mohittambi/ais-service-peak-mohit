'use strict';

const Repository = require('./repository');

class peak-mohitRepository extends Repository {
  constructor(entity) {
    this.entity = entity;
  }
}

module.exports = peak-mohitRepository;
